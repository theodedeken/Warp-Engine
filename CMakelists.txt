cmake_minimum_required(VERSION 3.10)
project (Warp)

#version
set(WARP_MAJOR_VERSION 0)
set(WARP_MINOR_VERSION 0)
set(WARP_PATCH_VERSION 0)
set(WARP_VERSION ${WARP_MAJOR_VERSION}.${WARP_MINOR_VERSION}.${WARP_PATCH_VERSION})

add_subdirectory(src)