cmake_minimum_required(VERSION 3.10)
project (OWAGE)

#version
set(OWAGE_MAJOR_VERSION 0)
set(OWAGE_MINOR_VERSION 0)
set(OWAGE_PATCH_VERSION 0)
set(OWAGE_VERSION ${OWAGE_MAJOR_VERSION}.${OWAGE_MINOR_VERSION}.${OWAGE_PATCH_VERSION})

add_subdirectory(src)